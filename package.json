{
  "name": "trash",
  "version": "1.0.3",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "flow:version": "npm version --no-git-tag-version",
    "flow:publish": "npm publish",
    "preversion": "git checkout develop && git flow release start v$npm_package_version",
    "postversion": "",
    "prepublish": "git flow release finish v$npm_package_version && git checkout master && latesttag=$(git describe --tags) && echo checking out ${latesttag} && git checkout ${latesttag}",
    "publish": "echo 'Published $npm_package_version'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/saxix/trash.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/saxix/trash/issues"
  },
  "homepage": "https://github.com/saxix/trash#readme"
}
